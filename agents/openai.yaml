interface:
  display_name: "Adgine Daily Feeds"
  short_description: "Fetch and render Chinese GEO/AEO daily reports"
  default_prompt: "Use $adgine-daily-feeds to fetch today's WeChat GEO/AEO daily report from the hosted API and render it if needed."
policy:
  allow_implicit_invocation: true
